/**
 * positive_or_negative - Computes the absolute value of an integer
 * @n: The integer to compute absolute value
 *
 * Return:  Absolute value of @n
 */
int positive_or_negative(int n)
{
	if (n < 0)
		n *= -1;

	return (n);
}
